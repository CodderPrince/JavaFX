#include<stdio.h>

int main(){
	int t,a,b,c,m,k,n,l;
	double area;

	scanf("%d",&t);
	while(t--){
	scanf("%d %d %d", &a, &b, &c);
		k=(a+b+c)/2;
		m=(k-a);
		n=(k-b);
		l=(k-c);

		area=sqrt(k*m*n*l);

	printf("%.3lf\n",area);
	}
	return 0;
}
